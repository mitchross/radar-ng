numpy>=1.26,<2
scipy>=1.12
opencv-python-headless>=4.10,<5
# 1.21+ fixes 'No module named distutils' on Python 3.12 (distutils
# removed from stdlib). 1.14 silently fell back to no-pysteps mode and
# the worker logged pysteps_unavailable on every nowcast_run, leaving
# /data/tiles/nowcast empty so Forecast mode rendered nothing.
pysteps>=1.21.0
Pillow>=12.3.0
mercantile>=1.2
