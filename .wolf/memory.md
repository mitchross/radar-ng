# Session Memory — radar-ng

| Time  | Description | Files | Outcome | ~Tokens |
|-------|-------------|-------|---------|---------|
| session-start | Tasks 6 & 7: React Query hooks, location hook, root layout, tab navigation | src/hooks/*, src/app/_layout.tsx, src/app/(tabs)/*, src/app/alert/[id].tsx | 16/16 tests pass, committed | ~800 |
| 18:16 | Fix 6 UI issues: ForecastSheet handle+peek, tab icons, PlayButton styled, RadarOverlay useMemo+per-source maxZoom, TimeSlider agoLabel+LIVE badge+frame counter | src/components/forecast/ForecastSheet.tsx, src/app/(tabs)/_layout.tsx, src/components/timeline/PlayButton.tsx, src/components/map/RadarOverlay.tsx, src/components/timeline/TimeSlider.tsx | tsc clean, 21/21 tests pass, committed | ~600 |
| 09:00 | Created WeatherMap.tsx + RadarOverlay.tsx, updated MapScreen | src/components/map/WeatherMap.tsx, src/components/map/RadarOverlay.tsx, src/app/(tabs)/index.tsx | All 16 tests pass, committed | ~200 |
| 23:02 | Tasks 10-11-12: created timeline, forecast, alerts components; updated map screen and alert detail; all 16 tests passed | src/components/timeline/*.tsx, src/components/forecast/*.tsx, src/components/alerts/AlertBanner.tsx, src/app/alert/[id].tsx, src/app/(tabs)/index.tsx | success | ~400 |
| 00:00 | Task 13: replaced placeholder settings.tsx with full settings screen (map style, temp unit, radar opacity, playback speed) | src/app/(tabs)/settings.tsx | committed fb43e8e, 16/16 tests pass | ~300 |
| 14:00 | Tasks 1-9: MMKV storage wrapper, types (SelfHostedManifest/LayerType/DataSource/LayerConfig), constants (SELF_HOSTED/LAYERS), updated store (layer/dataSource/serverUrl), updated useManifest (dual-source), self-hosted API functions, tileUrl builder, updated tests | src/lib/storage.ts (new), src/lib/api.ts, src/lib/constants.ts, src/lib/tileUrl.ts, src/types/weather.ts, src/stores/useWeatherStore.ts, src/hooks/useManifest.ts, src/components/map/RadarOverlay.tsx, __tests__/lib/api.test.ts, __tests__/stores/useWeatherStore.test.ts | 21/21 tests pass, tsc clean, committed 1fb8352 | ~1200 |
| 14:30 | Backend pipeline plan execution (all 7 tasks): color_tables.json, tiler.py+tests, ingest-mrms, ingest-hrrr, tile-server (Caddy+FastAPI), tile-cleanup, docker-compose, Dockerfile build context fix | services/**, deploy/docker-compose.yml | 2/2 tiler tests pass; bug fixed (ty_min/ty_max swap in render_tiles); committed dabc2b2 | ~2000 |
| 09:04 | Implemented 6 UI components: LayerPicker, WeatherLayerOverlay, AlertPolygon, DailyForecast; updated ForecastSheet, settings, index | LayerPicker.tsx, WeatherLayerOverlay.tsx, AlertPolygon.tsx, DailyForecast.tsx, ForecastSheet.tsx, settings.tsx, index.tsx | TS clean, 21/21 tests pass, committed | ~400 |
| 09:30 | Dark theme overhaul: NWS radar colors (scheme 6), opacity 0.8, zoom 8, dark mapStyle default, full-bleed map, translucent overlays, restyled ForecastPeek/TimeSlider/PlayButton/LayerPicker, slimmer tab bar, AlertBanner paddingTop 44. Updated 2 test assertions. | constants.ts, useWeatherStore.ts, index.tsx, ForecastSheet.tsx, TimeSlider.tsx, PlayButton.tsx, LayerPicker.tsx, _layout.tsx, AlertBanner.tsx, tileUrl.test.ts, useWeatherStore.test.ts | tsc clean, 21/21 tests pass, committed a0b59d1 | ~900 |
| 10:00 | Major UI overhaul: switched free tier from RainViewer to IEM NEXRAD (proper NWS green/yellow/red/magenta), redesigned bottom bar (floating timeline + separate forecast bar), removed all emoji icons (text letter icons + label-only tabs), slimmer alert banner, slimmer forecast peek | constants.ts, api.ts, tileUrl.ts, useManifest.ts, RadarOverlay.tsx, index.tsx, _layout.tsx, LayerPicker.tsx, AlertBanner.tsx, ForecastSheet.tsx, tileUrl.test.ts | tsc clean, 22/22 tests pass, committed a595c4d | ~1000 |
| 14:45 | CARROT Weather redesign: created forecast hero tab (index.tsx), moved radar to radar.tsx, updated _layout.tsx to 3 tabs, updated settings.tsx to dark card-based design, deleted 4 forecast components | src/app/(tabs)/_layout.tsx, index.tsx, radar.tsx, settings.tsx, src/components/forecast/* | 8 files changed, 22/22 tests pass, commit bd77a1b | ~800 |
| 21:45 | CARROT Weather v2: Created weatherTheme.ts (weather-adaptive gradients, temp font weight, temp colors, snarky quotes). Rebuilt index.tsx with LinearGradient bg, glassmorphism cards, personality quotes, chart-style hourly, gradient daily bars. Rebuilt _layout.tsx with View-based tab icons. Rebuilt settings.tsx with gradient bg. Added expo-linear-gradient. | src/lib/weatherTheme.ts (new), src/app/(tabs)/index.tsx, _layout.tsx, settings.tsx | tsc clean, 22/22 tests pass, building native | ~600 |
| 05:00 | CARROT Premium redesign: Browsed real CARROT Weather screenshots via agent-browser (App Store, Bing Images). Created WeatherScene.tsx (city skyline silhouettes + celestial objects). Enhanced weatherTheme.ts (5-stop gradients, SceneType, scene colors, getWindInfo/getUVInfo/getWindDirection). Rebuilt forecast screen with: weather scene header, 108px hero temp, Wind&Pressure compass card, Sun&Moon daylight progress, Atmosphere gauges (UV/humidity/dewpoint/pressure), precipitation chance bar chart. Updated types+API for dew_point_2m/surface_pressure/uv_index_max/precipitation_probability_max. | WeatherScene.tsx (new), weatherTheme.ts, index.tsx, api.ts, weather.ts | tsc clean, 22/22 tests pass, docker containers started | ~1200 |
| 06:00 | Radar screen CARROT redesign: Switched default map to light (OpenFreeMap positron). Created RadarFABs.tsx (white circle FABs: location arrow, layers toggle, map style toggle — matches CARROT's FAB style). Created TimelineBar.tsx (white bg, blue play button, blue slider, current time + LIVE badge, timestamp ticks). Rebuilt radar.tsx to use new components. Verified on emulator — light map + radar overlay looks dramatically better. | radar.tsx, RadarFABs.tsx (new), TimelineBar.tsx (new), useWeatherStore.ts (mapStyle default→light) | tsc clean, 22/22 tests pass, verified on emulator | ~400 |
| 15:20 | Phase 1 v3.0 plan: base Docker image (stormscope-base), self-hosted Open-Meteo service (open-meteo + sync profile), Protomaps basemap (go-pmtiles + positron/dark-matter styles, Caddyfile /basemap routes), backend hardening (JSON logger, retry+backoff, persistent processed-state, byte-range idx subsetting for HRRR), tile-server /api/health freshness + /api/metrics + OPEN_METEO_BASE proxy, app-side fetchForecast routes through server when dataSource=selfhosted, resolveMapStyleUrl helper, ingest-mrms+hrrr Dockerfiles FROM stormscope-base | services/base/*, services/shared/logger.py, services/shared/state.py, services/ingest-mrms/*, services/ingest-hrrr/*, services/tile-server/*, services/basemap/*, services/tile-cleanup/cleanup.sh, deploy/docker-compose.yml, src/lib/api.ts, src/lib/constants.ts, src/hooks/useForecast.ts, src/components/map/WeatherMap.tsx | tsc clean, 22/22 tests pass, compose valid | ~2500 |
| 17:30 | Cumulus redesign — full implementation of UI-Handoff/design_handoff_cumulus_weather | src/lib/cumulusTheme.ts, src/components/weather/WeatherIcon.tsx, src/app/(tabs)/_layout.tsx, src/app/(tabs)/index.tsx, src/app/(tabs)/nowcast.tsx, src/app/(tabs)/radar.tsx, src/app/settings.tsx, src/screens/NowcastScreen.tsx, src/components/timeline/TimelineBar.tsx, src/components/map/RadarFABs.tsx, src/lib/api.ts, src/lib/constants.ts, src/types/weather.ts, src/app/_layout.tsx | 3 tabs Home/Nowcast/Radar + settings modal; violet accent #8B7CFF; condition gradients; self-host path preserved (radar-hrrr layer + HRRR timeline segment); type-check clean, 22/22 tests pass | ~4500 |
| 15:00 | QC walkthrough via adb input-tap + screencap | src/app/(tabs)/_layout.tsx, src/app/(tabs)/radar.tsx, src/hooks/useManifest.ts, src/components/weather/WeatherIcon.tsx, src/screens/NowcastScreen.tsx | Found + fixed 5 UI bugs (bug-012..016) and 1 open backend bug (bug-017: self-host open-meteo returns null for most fields). Walkthrough covered Home/Nowcast/Radar/Settings + layer picker + pinpoint + play/scrub + mapStyle toggle + dataSource toggle. | ~2500 |
| 14:00 | Apple-Weather radar polish + Phase 3 (inspector, palettes, map themes): new LayerLocationMarker (user pill w/ layer-aware value + tail), LayerLegendCard (top-left vertical scale per layer), redesigned RadarFABs popover (layer-tinted bg + checkmarks + icons + scrim), MapStylePicker modal (flat/globe toggle + 3 style tiles), Eyedropper tap-to-pin (MarkerView + readout panel), TimelineBar header "Reflectivity / Sunday, April 19 2026", palettes (classic/vivid/muted JSON + shared/palettes.py loader, tile URL gains /palette/ segment, both ingestors render per-palette, manifest + cleanup.sh + server manifest endpoint handle dual layouts, PaletteSelector in Settings), Phase 3.1 /api/inspect endpoint + grid_dump.py (Float32 grid dumps from MRMS + HRRR, bilinear point sampling, 3MB downsample cap), globe projection preference persisted (render stays flat until MapLibre RN exposes native globe). | src/components/map/{LayerLocationMarker,LayerLegendCard,MapStylePicker,RadarFABs,WeatherMap}.tsx, src/components/inspector/Eyedropper.tsx, src/components/palette/PaletteSelector.tsx, src/components/timeline/TimelineBar.tsx, src/app/(tabs)/radar.tsx, src/app/settings.tsx, src/lib/{inspector,tileUrl,constants}.ts, src/stores/useWeatherStore.ts, src/types/weather.ts, services/shared/{palettes.py,grid_dump.py,palettes/*.json}, services/ingest-{mrms,hrrr}/ingest.py, services/tile-server/api/server.py, services/tile-cleanup/cleanup.sh | tsc clean, 22/22 tests pass, Python AST parse clean. Backend needs docker rebuild + re-ingest to serve non-classic palette tiles + populate grids for inspector. | ~4000 |
| 14:50 | Rebuilt + deployed Phase 3 containers, ran Phase 2 backend + frontend. Fixed MRMS lon wrap (0..360 → -180..180), fixed nowcast.py path (.meta.json → .bin), gcc added to nowcast Dockerfile for pysteps build, bumped pysteps to 1.14 for py3.12 distutils compat, fixed pysteps 1.14 R_thr→precip_thr rename with try/except. New nowcast service (pysteps S-PROG, 12 leadtimes +5..+60m) producing frames into /tiles/nowcast/{palette}/{ts}. App-side: TimelineMode store field ("current"|"forecast") persisted, CurrentForecastToggle component, useManifest merge past MRMS + nowcast + HRRR for radar family in forecast mode, RadarFrame.source optional tag so RadarOverlay pulls the right subtree per-frame. Emulator QC: radar legend + FABs + layer popover + location marker + Current/Forecast toggle all rendered cleanly. | services/nowcast/{nowcast.py,Dockerfile}, services/shared/grid_dump.py (lon fix), services/ingest-mrms/ingest.py (lon fix via write_grid), deploy/docker-compose.yml (PALETTES env, grids volume, nowcast service), services/ingest-{mrms,hrrr}/Dockerfile (env), src/lib/inspector.ts (<5dBZ → "No echo"), src/lib/tileUrl.ts (accept "nowcast" layer), src/types/weather.ts (TimelineMode, RadarFrame.source), src/stores/useWeatherStore.ts, src/hooks/useManifest.ts (merge), src/components/map/RadarOverlay.tsx (per-frame source subtree), src/components/timeline/CurrentForecastToggle.tsx (new), src/app/(tabs)/radar.tsx | All self-host containers up, manifest shows palettes + nowcast layer. pysteps S-PROG computing 12 leadtimes/iteration. 22/22 tests pass. | ~5500 |
| 15:20 | Phase 5 lightning + Phase 6 tropical + Phase 5 enhanced warnings. New ingest-lightning service (asyncio WebSocket → Blitzortung wss://ws2:443, LZW-or-raw-JSON decode, 15-min rolling GeoJSON buffer at /data/state/lightning.json). New ingest-tropical service (NHC CurrentStorms.json every 5min → merged FeatureCollection with position/track/cone per storm). New tile-server endpoints /api/lightning + /api/tropical reading those files. AlertPolygon upgraded to classify by event text (warning/watch/advisory/statement) with different outlines (solid/dashed/dotted) and fill opacities. App-side: useLightning + useTropical hooks, LightningOverlay (age-interpolated circle with halo for <60s strikes) + TropicalOverlay (cone fill + dashed track + position dot) wired into radar.tsx. Blitzortung-specific: only ws2 has a valid wss cert, other hosts reject; unprivileged ports (8087-90) are firewalled from this host — so primary path is wss://ws2. | services/ingest-{lightning,tropical}/{ingest.py,Dockerfile}, services/tile-server/api/server.py (+lightning/tropical endpoints, STATE_DIR env), deploy/docker-compose.yml (+2 services, state rw for ingestors, state:ro for tile-server), src/hooks/{useLightning,useTropical}.ts, src/components/map/{LightningOverlay,TropicalOverlay,AlertPolygon}.tsx, src/app/(tabs)/radar.tsx | tsc clean, 22/22 tests pass. Lightning flaky (Blitzortung LZW decoder works partially, frontend renders whatever the endpoint serves). Tropical endpoint returns empty collection correctly (0 active storms in April). | ~3000 |
| 21:30 | Phase 4 — precip accumulation + cloud cover + wind particles (Skia) + storm cells. New HRRR variables APCP (→precip-accum, in/hr) and TCDC (→cloud, %). Color tables in all 3 palettes. New LayerType entries wired through RadarFABs picker (umbrella+drop+cloud icons), LayerLegendCard (scale per layer), TimelineBar title, WeatherLayerOverlay rendering. Wind particles: @shopify/react-native-skia@2.6.2 (New-Arch compatible), new useWindField hook + sampleWindField worklet, WindParticlesOverlay (Skia canvas, 1200 particles in 4 speed bins as compound Paths, UI-thread advection via useFrameCallback, local web-mercator projection tied to SharedCamera from onRegionIsChanging). Separate U/V grid dumps in ingest-hrrr + new /api/wind-field endpoint (int8-scaled JSON, ~60kB payload). Storm cells: scipy.ndimage connected-component detection in ingest-mrms (threshold 40 dBZ, min 5 pixels), emits /data/state/storms.json, served via /api/storms. MRMS lon 0..360 → -180..180 normalization in storms.py. New StormCellsOverlay (CircleLayer halo + core, interpolated color/size by peak_dbz + area_km2). scipy added to stormscope-base. | services/ingest-hrrr/ingest.py (APCP+TCDC, U/V grids), services/ingest-mrms/ingest.py (+write_storms_json), services/shared/{palettes/*.json, storms.py (new)}, services/base/requirements.txt (+scipy), services/tile-server/api/server.py (+/api/wind-field,/api/storms, tile-cleanup expanded layer list), src/types/weather.ts (+precip-accum,cloud LayerType), src/lib/{constants.ts, inspector.ts}, src/components/map/{RadarFABs,LayerLegendCard,WindParticlesOverlay (new),StormCellsOverlay (new),WeatherMap,LayerLocationMarker}.tsx, src/components/timeline/TimelineBar.tsx, src/hooks/{useWindField,useStormCells}.ts (new), src/app/(tabs)/radar.tsx, package.json (+@shopify/react-native-skia) | tsc clean, 22/22 tests pass. 70 storm cells detected live, manifest + endpoints serve new layers. Skia needs `npx expo run:android` native rebuild on device. | ~4500 |
| 23:40 | fix wind particles: inline derived-value worklets, seed around camera, outline pass | src/components/map/WindParticlesOverlay.tsx | trails now visible on wind heatmap | ~3k |
